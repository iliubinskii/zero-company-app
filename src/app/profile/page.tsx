"use client";

import React from "react";
import { createPage } from "../../utils";

// eslint-disable-next-line no-warning-comments -- Postponed
// TODO: Add dashboard contents
const Page = createPage("/profile", () => <>TODO: Add dashboard contents</>);

export default Page;
