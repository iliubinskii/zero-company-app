import React from "react";
import { lang } from "../langs";

// eslint-disable-next-line no-warning-comments -- Postponed
// TODO: Style this
export const Fallback: React.FC = () => <div>{lang.Loading}</div>;
